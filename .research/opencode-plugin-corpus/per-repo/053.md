# Repository 053: OpenCode Chromium Browser Plugin

## Identification and Scope

- Manifest index `53` identifies **OpenCode Chromium Browser Plugin**, described as browser automation for Chromium browsers with a readable extension and native host (`.research/opencode-plugin-corpus/manifest.json:407-411`).
- The repaired repository map resolves the entry to `.research/opencode-plugin-corpus/repos/djocker-face--opencode-chromium-browser-plugin`, with the repository root and analysis path verified at commit `b221575674ac6b0ca5288c5e551bc51360b4c300` (`.research/opencode-plugin-corpus/repo-map.json:817-829`).
- The clone result is `succeeded` and points to the same collision-safe path (`.research/opencode-plugin-corpus/clone-results-49-97.json:59-67`).
- The inspected clone contains only `README.md` as a tracked file. There is no package metadata, OpenCode entrypoint, source directory, configuration, test suite, packaging configuration, or manifest subdirectory to inspect.

## Repository State

This is an archived redirect repository rather than the implementation repository. The README says the project “now lives” at `Quindart-com/opencode-chromium` and identifies `opencode-chromium` as the npm package name (`README.md:1-4`). It further states that source, releases, tags, and future updates are driven from the new repository, while this repository is archived for redirect purposes (`README.md:5-5`). The linked successor was intentionally not inspected because this analysis is restricted to the repository resolved for index 53.

## Evidence-Backed OpenCode Patterns

- **Use a stable redirect when moving a plugin repository.** The README preserves the old repository as a discoverable landing page and names the successor repository and npm package (`README.md:1-8`). This keeps existing links useful while moving active development elsewhere.
- **Separate source ownership from redirect metadata.** The repository explicitly disclaims active source, release, tag, and future-update ownership (`README.md:5-5`). For corpus analysis, the practical consequence is that the manifest URL is not necessarily the implementation source of truth, even when cloning succeeds.
- **Publish migration pointers in one place.** The README provides the successor GitHub URL, npm URL, and privacy-policy URL (`README.md:7-9`). A plugin migration should expose equivalent links so users can find code, installation artifacts, and policy documentation without guessing.

## Pitfalls and Security Lessons

- **Do not infer browser-automation behavior from the archived project description.** The manifest describes browser automation and a native host (`manifest.json:407-410`), but the clone contains no implementation. Claims about permissions, native messaging, browser flags, credential handling, remote control, or data flow would be unsupported for this repository.
- **Treat repository moves as an integrity boundary.** The README directs users to a different GitHub owner and an npm package (`README.md:3-8`). Consumers should verify that the successor repository, published package, release provenance, and requested OpenCode plugin name correspond before installing or updating; this clone provides no code or lockfile with which to validate that relationship.
- **Privacy documentation is only linked, not implemented here.** The README links a privacy policy (`README.md:9-9`), but no runtime source or policy file is present in the clone. The link is useful operationally, yet it cannot establish what data the archived repository would collect or transmit.

## Testing and Reliability Lessons

- **There is no executable test evidence in this repository.** No test files, test configuration, source, or package scripts are present; the only tracked file is the redirect README. Therefore test coverage, supported OpenCode SDK versions, browser compatibility, native-host failure behavior, and installation behavior cannot be assessed from this clone.
- **A successful clone is not proof of analyzable source.** The clone result reports success (`clone-results-49-97.json:59-67`), while the checked-out repository documents that implementation was removed during migration (`README.md:1-5`). Corpus pipelines should validate the tracked content and classify redirect-only repositories separately from source-bearing repositories.
- **The redirect itself is the only observable user path.** Its links should be checked for correctness and permanence as part of migration validation (`README.md:7-9`), but no runtime or integration test can be run against this repository.

## Packaging Lessons

- The README identifies the npm package as `opencode-chromium` (`README.md:3-4`) and links its npm page (`README.md:8-8`), but this clone has no `package.json`, build configuration, lockfile, published-file allowlist, or release workflow. Package entrypoints, dependency boundaries, Node/Bun requirements, and artifact completeness therefore remain unverified.
- The explicit statement that releases and future updates are driven from the successor (`README.md:5-5`) is a useful migration invariant: release automation and package metadata should be maintained in the active repository, not duplicated in an archived redirect.
- Installation guidance is intentionally delegated to the successor/npm links rather than duplicated here (`README.md:3-8`). That avoids stale instructions, but it also means the archived page is not sufficient to reproduce or audit installation.

## Bottom Line

Index 53 resolves correctly to a verified archived redirect at commit `b221575674ac6b0ca5288c5e551bc51360b4c300` (`repo-map.json:817-829`). The only repository-specific implementation evidence is the nine-line README: it establishes a clean repository/package migration pattern and exposes successor and privacy links, but it provides no basis for analyzing OpenCode hooks, browser/native-host security, tests, or packaging internals. Those topics require a separately scoped analysis of the successor repository, which is outside this report.
